<%= form_for(verse) do |f| %>
  <% if verse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(verse.errors.count, "error") %> prohibited this verse from being saved:</h2>

      <ul>
      <% verse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h2 class='h2'>Verse search</h2>
    <%= f.text_field :book, placeholder: 'book' %>
    <%= f.text_field :chapter, placeholder: 'chapter' %>
    <%= f.text_field :verse, placeholder: 'verse' %>
    <%= f.submit 'search' %>
  </div>
<% end %>
